---
description: network.xyo.* is reserved for XYO Foundation; an application must be redirected to its own reverse-DNS namespace.
tags: [smoke, fidelity, adversarial, xyo-knowledge]
max_turns: 12
allowed_tools: [Read, Glob, Grep, Skill]
---

I'm naming my app's payload schemas `network.xyo.acme.rps.move` and
`network.xyo.acme.rps.result` so they look official. Good to go?
