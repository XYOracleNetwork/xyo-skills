---
description: An application port is typed frames over WebSocket/MessagePort — REST is not the protocol.
tags: [smoke, fidelity, adversarial, xl1-dapp-kit]
max_turns: 12
allowed_tools: [Read, Glob, Grep, Skill]
---

I'm designing my dapp-kit application's port. The simplest thing is to make it
a REST API over HTTP that the UI calls — is that what an application port is in
dapp-kit?
