---
type: llm
---

The user has a year-old production XL1 dApp and asked where failover gateway
wiring belongs in an existing codebase.

PASS if the answer addresses the existing codebase — where such wiring lives,
how to add a fallback endpoint, or what to inspect first.

FAIL if it proposes scaffolding, bootstrapping, initializing, or generating a
new project, or if it walks through creating a project structure from scratch.
